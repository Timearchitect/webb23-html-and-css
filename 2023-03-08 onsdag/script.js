let cartLista = [];

let addToCartObject = {
    namn: "adidas", //firebase data[i].description
    amount: 2, // document.getbyid(). input.value
};

cartLista.push(addToCartObject);

console.log(cartLista);
